SELECT
    COALESCE(a.SG_ID_ENREGISTRMT, b.SG_ID_ENREGISTRMT) AS ID,
    COUNT(*) AS nb
FROM retour4C_B_HB a
FULL JOIN retour4Cinfo b
  ON TRIM(a.SG_ID_ENREGISTRMT) = TRIM(b.SG_ID_ENREGISTRMT)
GROUP BY 1
HAVING COUNT(*) > 1;